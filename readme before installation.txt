before installing, create the virtual environment to avoid dependency issues:s
1. Create a Virtual Environment (Recommended)
In your project directory:

python -m venv venv
venv\Scripts\activate  # For Windows